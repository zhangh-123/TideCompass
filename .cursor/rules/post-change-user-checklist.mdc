---
description: 代码改动后向用户提供需手动执行的清单
alwaysApply: true
---

# 改动后的用户执行清单

每当在本仓库完成**实质性代码改动**（尤其是已提交或即将交付给用户验收的改动）时，在回复末尾必须附带一节：

## 标题固定为：`你需要执行的清单`

清单内容根据本次改动实事求是列出，常见项包括但不限于：

1. **微信云开发**：哪些云函数需在开发者工具中「上传并部署」；是否需配置环境变量（如 `LLM_API_KEY`）。
2. **小程序客户端**：重新编译 / 预览 / 真机调试；清除缓存或删除开发者工具「缓存」后重试（若涉及本地存储逻辑）。
3. **依赖**：是否在项目根目录执行 `npm install`（若改过 `package.json`）。
4. **配置**：`project.config.json`、AppID、云环境 ID 是否与当前环境一致。
5. **Git**：若助手未推送，提醒用户 `git pull` / 合并分支等（若已推送则说明「代码已推送，拉取即可」）。

若无任何需要用户手动执行的事项，也要写明：**「本次改动无需额外手动步骤」** 或 **「仅需重新编译预览即可」**，避免留空。

不要用含糊表述；步骤写动词开头、可逐项勾选。
