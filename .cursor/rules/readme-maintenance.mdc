---
description: 重大改动时更新 README 并提交推送 Git
alwaysApply: true
---

# README 与 Git 同步

在本仓库（TideCompass）中工作时：

1. **重大改动**（例如新增主要功能模块、云函数架构调整、核心数据流或对外行为变化）：必须更新根目录 `README.md`，使项目描述、功能列表或开发与部署说明与当前实现一致。
2. 更新 README 后：**提交 Git**（清晰的提交说明），并 **`git push`** 到远程 `origin`，除非用户明确表示暂不推送。

较小改动不必强制每次改 README；若用户明确要求更新文档或改动本身显著影响使用方式，也应更新 README。
